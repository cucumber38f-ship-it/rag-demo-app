# AGENTS.md

## 基本方針

このリポジトリでは、明示的に許可されない限り、既存コードのロジックを変更しないこと。

レビュー時は、コードを直接修正するのではなく、指摘内容をコメントアウトとしてコード中に挿入すること。

## 許可される変更

- コメントアウトの追加のみ許可する
- 既存コードの削除・書き換えは禁止
- import文の追加は禁止
- 依存関係の追加は禁止
- フォーマット変更は禁止
- ファイル分割は禁止

## コメント形式

TypeScript / JavaScript の場合、次の形式でコメントすること。

```ts
// REVIEW: ここは req.body.query の型検証がないため、undefined や number が来た場合に想定外の動作になる可能性があります。
// SUGGESTION: typeof query === "string" && query.trim() !== "" を確認してください。